public class Globals {
    enum Difficulty {
        EASY,
        NORMAL,
        HARD
    }

    enum MenuOptions {
        START_GAME,
        SHOW_ME_A_RANDOM_WORD,
        EXIT
    }

    enum GameState {
        CONTINUE,
        EXIT
    }
}
