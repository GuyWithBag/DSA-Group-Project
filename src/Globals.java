public class Globals {
    enum Difficulty {
        EASY,
        NORMAL,
        HARD
    }

    enum MenuOptions {
        START_GAME,
        SHOW_ME_A_RANDOM_WORD,
        EXIT
    }

    enum GameState {
        MENU,
        GAMEPLAY,
        EXIT
    }

    enum GameMode {
        NORMAL,
        INFINITE_MODE
    }
}
